
// import { Pipe, PipeTransform } from '@angular/core';
// import { IPaginator, ListItems } from './../catalog.model';

// @Pipe ({
//     name: 'PaginatorPipe'
// })

// export class PaginatorPipe  implements PipeTransform {
//   transform(items: ListItems[], paginator: IPaginator) {
//     if (!items || !paginator) {
//       return null;
//     } else {
//       return items.slice(paginator.count*(paginator.page-1), paginator.count * paginator.page);
//     }
//   }
// }
